# Hybr1d

## if you dont have mongodb
    $ install docker https://docs.docker.com/engine/install/
    $ sudo docker-compose up -d  

## Usage
    $ npm install
    $ cp .env.example .env
    $ npm start
    
